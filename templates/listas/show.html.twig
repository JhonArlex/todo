{% extends 'base.html.twig' %}

{% block title %}Listas{% endblock %}

{% block body %}
    <h1>Listas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lista.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ lista.nombre }}</td>
            </tr>
            <tr>
                <th>Fecha_creacion</th>
                <td>{{ lista.fechaCreacion ? lista.fechaCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('listas_index') }}">back to list</a>

    <a href="{{ path('listas_edit', {'id': lista.id}) }}">edit</a>

    {{ include('listas/_delete_form.html.twig') }}
{% endblock %}
